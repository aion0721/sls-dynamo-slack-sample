{
  "scripts": {
    "invoke:local": "sls invoke local --function dynamoStream --path ./lib/data.json",
    "invoke:aws": "sls invoke --function dynamoStream",
    "deploy": "sls deploy"
  },
  "devDependencies": {
    "serverless-dotenv-plugin": "^2.4.2"
  },
  "dependencies": {
    "@slack/web-api": "^5.9.0"
  }
}
